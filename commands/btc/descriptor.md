---
description: My wallet's output descriptor (public, for watch-only import)
---
Show my wallet's output descriptor using wallet_descriptor (public, safe to share, no private key material) so I can import it as watch-only into Bitcoin Core.
